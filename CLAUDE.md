# Resource Planning Tool · Claude Code 入口

Claude Code 进入本仓库后，先读取 [`AGENTS.md`](./AGENTS.md)。项目级规范以 `AGENTS.md` 为单一事实源，包括：

- 可用 Skills 与使用方式（superpowers-zh 框架）
- **Worktree 开发流程**（强制：开发必须走 worktree，不在 `main` 上直接改代码）
- 标准开发流程（设计 → TDD → 验证 → 收尾合并）

本文件只作为 Claude Code 的入口桥接，不重复维护 skill 清单或开发流程。具体开发规范与运维细节以 [`AGENTS.md`](./AGENTS.md) 为准。
